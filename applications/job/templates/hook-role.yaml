{{- if or (ne .Values.image.repository "ghcr.io/porter-dev/porter/hello-porter-job") (.Values.global) -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "docker-template.fullname" . }}
rules:
  - apiGroups:
      - batch
    resources:
      - jobs
    verbs:
      - get
      - watch
      - list
      - create
      - delete
{{- end }}
