upgrade_notes:
- previous: v0.19.0
  target: v0.20.0
  note: |
    `v0.20.0` added support for configuring the time interval of the readiness probe. This sets the period between successive readiness probe queries on the health check endpoint.
