upgrade_notes:
- previous: v0.8.0
  target: v0.9.0
  note: |
    `v0.9.0` added support for displaying the revision numbers attached to each pod in the Porter dashboard, by passing the revision number to the pod metadata. This update is backwards-compatible.