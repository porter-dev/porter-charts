---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: {{ include "ack-cloudtrail-controller.app.fullname" . }}-writer
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "ack-cloudtrail-controller.app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    k8s-app: {{ include "ack-cloudtrail-controller.app.name" . }}
    helm.sh/chart: {{ include "ack-cloudtrail-controller.chart.name-version" . }}
rules:
- apiGroups:
  - cloudtrail.services.k8s.aws
  resources:
  - eventdatastores
  - trails
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - cloudtrail.services.k8s.aws
  resources:
  - eventdatastores
  - trails
  verbs:
  - get
  - patch
  - update
