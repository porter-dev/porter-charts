kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: efs-{{ .Release.Name }}-deepgram
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  directoryPerms: "700"
  fileSystemId: {{ .Values.efsFileSystemId }}
  reuseAccessPoint: "true"