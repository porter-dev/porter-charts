apiVersion: v1
kind: Service
metadata:
  labels:
    llm-model: {{ .Release.Name }}
  name: {{ .Release.Name }}
spec:
  ports:
    - name: https
      port: 8000
      targetPort: https
  selector:
    llm-model: {{ .Release.Name }}